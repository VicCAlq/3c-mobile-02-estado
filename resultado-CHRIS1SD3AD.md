Data de atualização: Dia 27/8/2026 às 11:41 

| Questão | Teste | Entregue | Correto | Mensagem de Erro |
|---------|-------|----------|---------|------------------|
| AtvCumprimenta | Renderiza o componente como esperado | ✅ | ❌ | Error: Invariant Violation: Text strings must be rendered within a <Text> component. Detected attempt to render "Salvar" string within a <View> component. |
| AtvCumprimenta | mostra a pergunta e cumprimenta o usuário com o nome digitado | ✅ | ❌ | Error: Invariant Violation: Text strings must be rendered within a <Text> component. Detected attempt to render "Salvar" string within a <View> component. |
| AtvMudaConteudo | Altera para a aba da página em construção | ✅ | ❌ | Error: Invariant Violation: Text strings must be rendered within a <Text> component. Detected attempt to render " a " string within a <View> component. |
| AtvMudaConteudo | alterna entre o texto da primeira aba e os blocos coloridos da segunda | ✅ | ❌ | Error: Invariant Violation: Text strings must be rendered within a <Text> component. Detected attempt to render " a " string within a <View> component. |
| AtvDobra | Renderiza o componente como esperado | ✅ | ❌ | Error: Invariant Violation: Text strings must be rendered within a <Text> component. Detected attempt to render "Dobrar número" string within a <View> component. |
| AtvDobra | exibe o número recebido e dobra o valor a cada clique no botão | ✅ | ❌ | Error: Invariant Violation: Text strings must be rendered within a <Text> component. Detected attempt to render "Dobrar número" string within a <View> component. |
| /AtvTelefoneSemFio.test.jsx | ❌ | ❌ | ❌ | Atividade não feita ou não exportada corretamente |
